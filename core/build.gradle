apply plugin: 'me.champeau.jmh'

description = 'Externalized Properties Core.'

jar {
  manifest {
    attributes(
      'Automatic-Module-Name': 'io.github.jeyjeyemem.externalizedproperties.core'
    )
  }
}